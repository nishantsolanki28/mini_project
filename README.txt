The following code implements two pass assembler of SIC architecture. 
The input is given through a file and corresponding optab is along with the file.
To get different outputs only the contents of the file needs to be modified.
The code scans the input file and makes the intermediate file,symbol table file,output file with corresponding object code.
	
 BY:
   Nishant Singh Solanki           
   ROLL NO-30(I.T)                     
   UNIV. ROLL NO.-2007711           
					
